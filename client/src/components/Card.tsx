import React from 'react';
import {Box, Typography} from '@mui/material';

const Card = () => {
    return(
        <Box>
            <Typography>This is a card</Typography>
        </Box>
    );
}

export default Card;